/**
 * Frontend controller-layer mapping (thin wrappers / UI controllers).
 * This folder mirrors the backend `controllers` folder structure for clarity.
 * Place UI-specific controller logic here if needed (e.g. composition of services + state).
 */

export {};
